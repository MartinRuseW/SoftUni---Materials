function isExcellent(input) {
    let grade = Number(input[0]);
    if (grade >= 5.50) {
        console.log("Excellent!")
    }
	// Comment from branch
}
	// Comment from master branch
isExcellent(["5.50"]);